This folder is intentionally empty.

Model artifacts (kmeans.pkl, standard_scaler.pkl, selected_features.pkl,
pipeline.pkl) are loaded directly from
`../06-Customer-Personality-Analysis/outputs/models/` — see `src/config.py`.
They are not duplicated here to avoid redundant large-file copies.
